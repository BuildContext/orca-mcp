# NAS-258 reconcile onto 0.3.3 (cut 0.3.4)

Worker: `task_9d9f586ae54c` / `ctx_bfedc4d60376`. Checkout
`BuildContext/nas258-reconcile-s7` from main `0a82468` (0.3.3).

## (a) Base sanity

`/tmp/s7-nas258/base-server.mjs` is **byte-identical** to
`git show v0.3.2:server.mjs` (md5 `19574f91b02171b9c4fe1fd326bd356f`,
130613 bytes). HEAD `server.mjs` is also identical to v0.3.2 — 0.3.3 did
not touch it (NAS-257/260 were unit + cli-policy). Safe to re-apply the
0.3.2-authored delta onto this branch.

## (b) Hunk classification (base → live `server.mjs`)

All hunks are against `server.mjs` unless noted.

| # | Hunk | Class | Reason |
|---|------|-------|--------|
| 1 | Imports `verifyWorkerCapability`, `workerCapabilityEnv`, `lib/worker-cap-file.mjs`, `lib/worker-orch-relay.mjs` | (a) | Required for mint/file/relay. |
| 2 | Recovery prompt: cap file path instead of `ORCA_WORKER_CAPABILITY` env | (a) | File delivery, not env. |
| 3 | Recovery `worker_done` template adds `--from ${handle}` | (a) | Isolated worker identity. |
| 4 | `workerBridgeOrigin`, `sudoSeedWorkerCreds`, `writeAtomic0600`, `mintAndInstallWorkerCap`, `markWorkerCapPurge` | (a) | Mint + purge implementation. |
| 5 | Pass `taskId` into `planIsolatedAgentPlacement` | (a) | Wrapper `--task-id` for by-task pointer. |
| 6 | Isolated path: `dispatch --return-preamble` **before** grok; legacy path unchanged | (a) | Need `dispatchId` to name the cap file. |
| 7 | Mint via `mintAndInstallWorkerCap` (file) instead of in-memory token only | (a) | Core NAS-258. |
| 8 | Isolated: wait tui-idle, then `terminal send` of bind preamble | (a) | Bind skipped `--inject`. |
| 9 | `extra: { workerIsolation }` on dispatch registry | (a) | Release skips shared-cred purge if another isolated dispatch is live. |
| 10 | Dispatch response `worker_capability_file` | (a) | Required live property. |
| 11 | `purgeWorkerCreds` hook into `executeReleaseWorker` | (a) | Required live purge. |
| 12 | `handleWorkerOrchHttp` + `POST /worker/orch` before bearer gate | (a) | 994 has no `ORCA_BRIDGE_TOKEN`. |

No (c) unrelated drift in `server.mjs`.

**Not shipped (b) session scaffolding** from `BuildContext/nas258-finish`:

| File | Why excluded |
|------|----------------|
| `docs/research/NAS-258-APPLY-LOG.md` | Live-apply session notes. |
| `docs/research/NAS-258-COMPLETION-LOG.md` | Session completion log. |
| `docs/research/NAS-258-SUDOERS-NARROW-DRAFT.md` | Draft commentary; live sudoers fragment is the deploy file below. |

**Shipped with server (supporting (a), not server.mjs hunks):**
`lib/worker-cap-file.mjs`, `lib/worker-orch-relay.mjs`, isolation
`--task-id`, release purge hook, tests, seed helper + wrappers + tmpfiles
+ exact-command sudoers fragment + installer wiring. These already match
live `/opt/orca-mcp` (finish branch == live).

## (c) Files changed in this checkout

- `server.mjs`
- `lib/worker-isolation.mjs`
- `lib/worker-isolation.test.mjs`
- `lib/release-worker.mjs`
- `lib/release-worker.test.mjs`
- `lib/worker-cap-file.mjs` *(new)*
- `lib/worker-cap-file.test.mjs` *(new)*
- `lib/worker-orch-relay.mjs` *(new)*
- `lib/worker-orch-relay.test.mjs` *(new)*
- `deploy/linux/orca-seed-worker-creds` *(new)*
- `deploy/linux/orca-as-worker` *(new)*
- `deploy/linux/orca-omp-as-worker.sh`
- `deploy/linux/orca-worker-orch.sh`
- `deploy/linux/tmpfiles-orca-mcp-worker-caps.conf` *(new)*
- `deploy/linux/orca-mcp-seed-creds.sudoers` *(new; repo deploy artifact only — `/etc` not touched)*
- `deploy/linux/install-worker-uid.sh`
- `package.json` (0.3.4 + files list)
- `package-lock.json`
- `server.json` (two version fields + ghcr tag)
- `CHANGELOG.md`
- `docs/research/NAS-258-0.3.4-reconcile.md` *(this file)*

## (d) Four live properties, in the landed code

1. **`worker_capability_minted: true`** — `server.mjs` dispatch envelope
   `worker_capability_minted: Boolean(workerCapability)` after
   `mintAndInstallWorkerCap`.
2. **`worker_capability_file: /run/orca-mcp/worker-caps/<dispatchId>.json`**
   — `workerCapFile = capFilePath(dispatchId)`.
3. **uid 994 / mode 0600** — staging write is `writeAtomic0600` (mode
   `0o600`); `orca-seed-worker-creds seed` `chown 994:994` + `chmod 0600`
   before `mv` to the dest. `worker-caps` dir is `0711` (no sibling list).
4. **`release` purges** — `purgeWorkerCreds` → `markWorkerCapPurge` →
   seed processes `*.purge` → `credential_purge.cap.ok`. Live inject-path
   release is the terminal-close fallback (this is where purge runs).

## (e) Suite / lint / docs

- `npm test`: **545/545 pass, 0 fail** (baseline 527 + 18 NAS-258).
- `npm run lint`: **34/34 ok**.
- `npm run docs:check`: **ok**.

## (f) Unsafe-but-landed (not silently improved)

- **Purge only on terminal-close**, not on successful `worker-release`.
  Inject-path live releases take close; a future `worker-release` hit
  would leave the cap file. Landed faithfully.
- **`current-cap.json` is a single HOME pointer** (last mint wins).
  Per-dispatch files under `/run/orca-mcp/worker-caps/` are the real
  authority.
- **`seed` is overloaded**: cap install + copy of
  `/home/orca/.grok/auth.json` → worker HOME. Same exact sudoers verbs
  (`seed`/`purge`); no `env_keep`, no new verb.
- **`POST /worker/orch` is pre-bearer**, HMAC-only. Intended: 994 must
  not hold `ORCA_BRIDGE_TOKEN`.
- Capability token lives in the 0600 file (not env). `workerCapabilityEnv()`
  is the field map for that file, not process-env delivery.
